document.addEventListener('DOMContentLoaded',()=>{const input=document.getElementById('selectedTable'),btn=document.getElementById('continueBtn');document.querySelectorAll('.seat:not(.blocked)').forEach(seat=>seat.addEventListener('click',()=>{document.querySelectorAll('.seat').forEach(s=>s.classList.remove('selected'));seat.classList.add('selected');input.value=seat.dataset.id;btn.disabled=false;}));});
